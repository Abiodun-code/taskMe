import { Onboarding } from "@/screens/not-authenticated";
import { NOT_AUTHENTICATED_PATH } from "@/types/notAuthenticatedType";

export const NOT_AUTHENTICATED_STACK = [
  {
    id:0,
    name: NOT_AUTHENTICATED_PATH.Onboarding,
    screen: Onboarding
  }
]